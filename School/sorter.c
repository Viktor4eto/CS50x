#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main(void)
{
    int sum = 0;
    int n;
    do
    {
        n = get_int("n: ");
    } while (n < 0);

    for(int i = 0; i < n - 1; i++)
    {
        for (int j = i + 1; )
    }
    printf("%i \n", sum);
}