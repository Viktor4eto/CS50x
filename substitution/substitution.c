#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

char substitute(char x);

int main(int argc, string argv[])
{
    toupper(argv[1]);
}

char substitute(char x)
{
    int i = (int) x;
    if (argv[1][i])
    char new = argv[1][i - ]
}